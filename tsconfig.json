{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "strict": true,
    // Path alias: @/ = src/ (avoids ../../../ in imports) 
    "baseUrl": ".",
    "paths": {
      "@/*": ["src/*"]
    }
  }
}
